<% @users.each do |user| %>
  <div class="user-card">
    <div class="img-plch"></div>
    <div class="user-detls box">
      <h2><%= user.name %></h2> 
      <span>Number Of Posts: <%= user.posts_counter %></span>
    </div>
  </div>
<% end %>
